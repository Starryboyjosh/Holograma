# ============================================================
# Holograma UNEV — requirements.txt
# Regla de Oro C: Automatiza el entorno virtual.
#
# Instalación (Linux/macOS):
#   python -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt
#
# Instalación (Windows PowerShell):
#   py -m venv .venv
#   .\.venv\Scripts\pip install -r requirements.txt
# ============================================================

# === Core: cliente OpenAI (NVIDIA NIM, OpenAI, etc.) ===
openai>=1.0

# === STT: Faster-Whisper + micrófono (Regla B: sounddevice, no pyaudio) ===
faster-whisper>=1.1.0
sounddevice>=0.5.0
numpy>=1.24
nvidia-cublas-cu12>=12.0
nvidia-cudnn-cu12>=9.0

# === Visión: YOLOv8/v11 + OpenCV (cross-platform) ===
ultralytics>=8.3
opencv-python>=4.10

# === TTS: Piper (opcional — descomentar si se desea) ===
# piper-tts

# === Setup: herramientas interactivas ===
rich>=13.0
