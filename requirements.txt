# ============================================================
# Holograma UNEV — requirements.txt
# Regla de Oro C: Automatiza el entorno virtual.
#
# Instalación (Linux/macOS):
#   python -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt
#
# Instalación (Windows PowerShell):
#   py -m venv .venv
#   .\.venv\Scripts\pip install -r requirements.txt
# ============================================================

# === Core: cliente OpenAI (NVIDIA NIM, OpenAI, etc.) ===
openai>=2.40.0

# === STT: Faster-Whisper + micrófono (Regla B: sounddevice, no pyaudio) ===
faster-whisper>=1.2.0
sounddevice>=0.5.5
numpy>=2.0.0
# GPU NVIDIA opcional en Linux/Windows x86_64:
#   pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
# No se incluyen por defecto para mantener compatibilidad con CPU y Raspberry Pi.

# === Visión: YOLOv26 + OpenCV (cross-platform) ===
ultralytics>=8.4
opencv-python>=4.13.0

# === TTS: Piper (opcional — descomentar si se desea) ===
piper-tts>=1.3.0; platform_machine == "x86_64" or platform_machine == "AMD64"

# === Setup: herramientas interactivas ===
rich>=13.0

# === API & WebSockets & LLM streaming ===
anthropic>=0.105.0
fastapi>=0.136.0
uvicorn>=0.49.0
python-dotenv>=1.0.0
